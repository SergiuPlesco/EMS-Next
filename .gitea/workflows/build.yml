name: Build Checks

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v3

      - uses: pnpm/action-setup@v2
        name: Install pnpm

      - name: Setup Nodejs
        uses: actions/setup-node@v3
        with:
          node-version: lts/*
          cache: 'pnpm'
      
      - name: Build
        run: |
          npm install
          npm run build                                        

      - name: Notify
        uses: teknatha136/actions-google-chat-text-message@main
        with:
          google-chat-webhook: ${{ secrets.GOOGLE_CHAT_WEBHOOK }}
          text-message: "Smartskills Build Checks <https://git.s4.smartdata.solutions/${{ gitea.repository }}/actions/runs/${{ gitea.run_number }}|#${{ gitea.run_number }}> for branch *${{ gitea.head_ref || gitea.ref_name }}*, *${{ gitea.event_name }}* by *${{ gitea.actor }}*\nStatus: *${{ job.status }}*"
        if: always()