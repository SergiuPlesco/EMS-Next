import React from "react";
// import { trpc } from "@/utils/trpc";

const HomePage = () => {
  // const { data, isLoading } = trpc.users.all.useQuery();
  // const { data: skills } = trpc.skills.skillByUserId.useQuery();
  // console.log(skills);
  return <div>Content</div>;
};

export default HomePage;
